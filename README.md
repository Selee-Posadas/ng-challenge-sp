Instalar dependencias:
npm i

Configurar variables de entorno:
Crea un archivo .env en la raíz del proyecto y añade la URL base de la API:

cp .env.template

Ejecutar en modo desarrollo:

npm run dev

Desarrollado con ❤️ por Selene Posadas - 2026